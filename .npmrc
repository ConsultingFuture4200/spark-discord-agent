# esbuild (pulled in by vitest) needs its postinstall to place its native binary.
# Without this, pnpm's ignored-builds gate makes the pre-run deps check fail.
enable-pre-post-scripts=true
verify-deps-before-run=false
